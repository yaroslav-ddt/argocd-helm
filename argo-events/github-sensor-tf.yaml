apiVersion: argoproj.io/v1alpha1
kind: Sensor
metadata:
  name: github-tf
spec:
  template:
    serviceAccountName: operate-workflow-sa
  dependencies:
    - name: test-dep
      eventSourceName: github
      eventName: example
      filters:
        data:
          # Type of Github event that triggered the delivery: [pull_request, push, issues, label, ...]
          # https://docs.github.com/en/developers/webhooks-and-events/webhook-events-and-payloads
          - path: headers.X-Github-Event
            type: string
            value:
              - push
        exprs:
          - expr: modified =~ "apps-tf"
            fields:
              - name: modified
                path: body.commits.0.modified.0 #body.commits.#(modified.#(%"apps-tf*"))#.modified.0
  triggers:
    - template:
        name: github-workflow-trigger
        k8s:
          operation: create
          source:
            resource:
              apiVersion: argoproj.io/v1alpha1
              kind: Workflow
              metadata:
                generateName: github-              
              spec:
                entrypoint: main 
                arguments:
                  parameters:
                    - name: repo-url
                    - name: oidc_issuer_url
                    - name: virtual_network_subnet_ids
                templates:
                  - name: git
                    inputs:
                      parameters:
                        - name: repo-url
                          value: "{{workflow.parameters.repo-url}}"
                      artifacts:
                        - git:
                            repo: "{{inputs.parameters.repo-url}}"
                            singleBranch: true
                            branch: main
                            depth: 1
                          name: tfconfig
                          path: /tfconfig
                    container:
                      image: docker/whalesay:latest
                      command: [cowsay]
                      args: ["{{inputs.parameters.repo-url}}"]
                    outputs:
                      artifacts:
                        - name: tfconfig
                          path: /tfconfig
                  - name: tf-app
                    serviceAccountName: terraform-app-sa
                    inputs:
                      parameters:
                        - name: oidc_issuer_url
                        - name: virtual_network_subnet_ids
                    metadata:
                      labels:
                        azure.workload.identity/use: "true"              
                    container:
                      image: cdpargoyy.azurecr.io/cdpargoyy/argo-app:a89a7f491feb56e7d74b16dc4b379ee4ddb087a1
                      name: tf-app
                      command:
                        - sh
                        - entrypoint.sh
                      args:
                        - terraform
                        - plan
                        - "-var-file=/tfconfig/apps-tf/testapp/config.tfvars"
                        - "-input=false"
                        - "-out=/tfplan/tfplan"
                        - "-var oidc_issuer_url={{inputs.parameters.oidc_issuer_url}}"
                        - "-var virtual_network_subnet_ids=[{{inputs.parameters.virtual_network_subnet_ids}}]"
                      env:
                        - name: ARM_SUBSCRIPTION_ID
                          valueFrom:
                            configMapKeyRef:
                              name: tf-configmap
                              key: az_subscription_id
                        - name: STATE_RG
                          valueFrom:
                            configMapKeyRef:
                              name: tf-configmap
                              key: state_rg
                        - name: STATE_SA
                          valueFrom:
                            configMapKeyRef:
                              name: tf-configmap
                              key: state_sa
                        - name: STATE_CONTAINER
                          valueFrom:
                            configMapKeyRef:
                              name: tf-configmap
                              key: state_container
                        - name: OIDC_ISSUER_URL
                          valueFrom:
                            configMapKeyRef:
                              name: tf-configmap
                              key: oidc_issuer_url
                        - name: APP_NAME
                          value: "testapp"
                  - name: main
                    dag:
                      tasks:
                        - name: git
                          template: git
                        - name: tf-plan
                          template: tf-app
                          arguments:
                            parameters:
                              - name: oidc_issuer_url
                                valueFrom:
                                  configMapKeyRef:
                                    name: tf-configmap
                                    key: oidc_issuer_url
                              - name: virtual_network_subnet_ids
                                valueFrom:
                                  configMapKeyRef:
                                    name: tf-configmap
                                    key: virtual_network_subnet_ids
          parameters:
            - src:
                dependencyName: test-dep
                dataKey: body.repository.html_url
              dest: spec.arguments.parameters.0.value
      retryStrategy:
        steps: 3