apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: sample-project
  namespace: argocd
spec:
  clusterResourceWhitelist:
  - group: '*'
    kind: '*'
  destinations:
  - namespace: sample-app
    server: https://kubernetes.default.svc
  orphanedResources:
    warn: false
  sourceRepos:
  - '*'
  roles:
  - name: dev
    description: DevOps privileges on the namespace
    policies:
    - p, proj:sample-project:dev-ops, applications, get, sample-app/*, allow
    - p, proj:sample-project:dev-ops, applications, sync, sample-app/*, allow
    - p, proj:sample-project:dev-ops, applications, delete, sample-app/*, allow
    - p, proj:sample-project:dev-ops, applications, action/*, sample-app/*, allow
    - p, proj:sample-project:dev-ops, logs, get, sample-app/*, allow
    - p, proj:sample-project:dev-ops, exec, create, sample-app/*, allow
